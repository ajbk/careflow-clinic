import type { ClinicalNoteAmendmentDto, SignedClinicalNoteDto, SignedMedicationDecisionDto } from "../../../shared/contracts";
import { formatThaiDateTime } from "../../lib/thai-date";
import { ActionButton } from "./ui";
export function SignedClinicalEvidence({ note, decision, amendments, onAmend, onRevise }: { note: SignedClinicalNoteDto; decision: SignedMedicationDecisionDto; amendments: ClinicalNoteAmendmentDto[]; onAmend?: () => void; onRevise?: () => void }) { return <section className="signed-evidence" aria-label="หลักฐานลงนาม"><h2>หลักฐานที่ลงนามแล้ว</h2><p>ผู้ลงนาม {note.signedBy.displayName} · {formatThaiDateTime(note.signedAt)} · เวอร์ชัน {note.version}</p><code>{note.contentHash}</code><p>การตัดสินใจยา: {decision.kind === "ORDER" ? "สั่งยา" : "ไม่สั่งยา"} · เวอร์ชัน {decision.version}</p>{amendments.map((amendment) => <p key={amendment.id}>คำแก้ไข v{amendment.version}: {amendment.reason}</p>)}<div className="decision-choice">{onAmend ? <ActionButton type="button" variant="secondary" onClick={onAmend}>เพิ่มคำแก้ไข</ActionButton> : null}{onRevise ? <ActionButton type="button" variant="secondary" onClick={onRevise}>แก้ไขการตัดสินใจยา</ActionButton> : null}</div></section>; }
